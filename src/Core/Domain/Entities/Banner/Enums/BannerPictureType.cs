namespace Domain.Entities.Banner.Enums
{
    #region (Enums)

    public enum BannerPictureType
    {
        [Display(Name = "مربع")]
        Square,
        [Display(Name = "مستطیل")]
        Rectangle
    }
    #endregion
}
